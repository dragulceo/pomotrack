<div class="btn-group-vertical" role="group" aria-label="...">
    {{#each item in projects}}
      {{#link-to 'clients.projects.tasks' item tagName='button' class='btn btn-default'}}{{item.title}}{{/link-to}}
    {{/each}}
</div>

{{outlet}}
